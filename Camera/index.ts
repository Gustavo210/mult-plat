import { CameraRoot } from "./src/components/CameraRoot";
import { Header } from "./src/components/Header";
import { Overlay } from "./src/components/Overlay";

export const Camera = Object.assign(CameraRoot, {
  Header,
  Overlay,
});
